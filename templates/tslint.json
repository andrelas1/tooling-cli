{
  "extends": [
    "tslint:recommended",
    "tslint-config-prettier"
  ],
  "rules": {
    "max-line-length": [true, {
      "limit": 120,
      "ignore-pattern": "^import |^export {(.*?)}"
    }],
    "indent": [
      true,
      "spaces",
      2
    ],
    "no-console": false
  }
}